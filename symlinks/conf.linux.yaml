- clean: ['~']

- defaults:
    link:
      create: true
      relink: true

- link:
    ~/.dotly: os/linux/.dotly
    ~/.gitalias: git/.gitalias
    ~/.gitconfig: git/.gitconfig
    ~/.gitignore: git/.gitignore
    ~/.gitkeep: git/.gitkeep
