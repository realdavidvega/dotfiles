# Points Syncthing's config at /srv instead of the ecryptfs home.
# Without this the service starts at boot but cannot read its own config,
# because an encrypted home only unlocks when PAM receives a login password.
[Service]
Environment=STCONFDIR=/srv/services/syncthing
Environment=STDATADIR=/srv/services/syncthing
